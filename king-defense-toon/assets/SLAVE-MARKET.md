# Slave market

Approved building design: `../design/buildings/slave-market-v1.png` (1536 × 1024 RGBA, 1,420,967 bytes). The original was generated by editing the user's Tiny Swords Free Pack blue Barracks sprite; its original art and this approved PNG remain unchanged. The exact image-generation prompt is recorded in `../design/buildings/slave-market-v1.prompt.md`.

Runtime asset: `web/slave-market.webp`, 320 × 213 RGBA, **51,500 bytes** (50.3 KiB; 96.4% smaller than the approved PNG). This retains the complete canvas, including transparent padding, canopy, trade emblem, counter, and fenced courtyard.

Export with the repository's existing Sharp dependency:

```js
await sharp('design/buildings/slave-market-v1.png')
  .resize({ width: 320, height: 320, fit: 'inside', withoutEnlargement: true, kernel: 'lanczos3' })
  .webp({ lossless: true, effort: 6 })
  .toFile('assets/web/slave-market.webp');
```

Compression is lossless relative to the one-time resized image. Decoded visible RGB and alpha match that resized image exactly; alpha is retained and all four corners remain transparent. The export was visually inspected at runtime size and at 120 px menu width on a cream background. No new artwork or runtime image processing is required.

SHA-256: `bf60090fb9cecbe2856f9c361a6d34d10c7a616ddfe9a7980cc31aa157d4ec04`.
