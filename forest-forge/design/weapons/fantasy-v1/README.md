# Fantasy weapons — three-item preview

The revised preview is `concepts-simplified.png`, generated with the built-in image_gen tool from `concepts.png` after the user requested less detail. It preserves the three identities and removes moss, surface texture, small facets and intricate joints in favor of broad color shapes. The exact edit prompt is `simplified-prompt.txt`. It is still a preview, not a fitted runtime asset.

Generated with the built-in image_gen tool. The user reduced the proposed six-item batch to three items. This is a concept preview, not yet fitted or added to the game's weapon pool; no epoch assignments are finalized.

Left to right in `concepts.png`:

1. Titan Fist — melee; a giant petrified fist with a short wrapped wrist grip; existing swing pose.
2. Demon Wing — melee; a large membranous wing with hooked bone tips and a wrapped forearm grip; existing swing pose.
3. Bombardier Beetle — ranged; the beetle itself forms the weapon, with a large abdomen, forward mouth/nozzle and folded belly-leg grip; existing horizontal shooting pose.

`concepts.png` is the unchanged generated sheet. The exact prompt is saved in `generation-prompt.txt`. The drawing-style references were `design/sets/underworld/concepts.png` and `design/sets/divine/concepts.png`; their subjects were not requested as output.

The preview emphasizes three different silhouettes: a compact heavy fist, a tall open wing fan, and a low horizontal insect shape. Any subsequent fitting should preserve those proportions and use the existing animations.
