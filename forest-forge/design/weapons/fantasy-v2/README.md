# Fantasy weapons — new trio

The current revised preview is `concepts-distinct-grips.png`. The built-in image_gen edit replaces the repeated leather-and-gold grips with a shark tail, an uninterrupted mushroom stem with a rounded root base, and a curved burgundy horn, respectively. It preserves the original weapon heads and drawing style. The exact edit prompt is `distinct-grips-prompt.txt`. This revision is still a concept preview, not a runtime asset.

Generated with the built-in image_gen tool after the user requested different weapons in the established game style. The direct drawing references were `../medieval/war-hammer-clean.png` and `../space/comet-launcher-clean.png`.

Left to right in `concepts.png`: Shark Cleaver (melee), Toadstool Maul (melee), Dragon-Mouth Cannon (ranged). The unchanged generated sheet is a concept preview; these weapons are not fitted, assigned to epochs, or integrated into the runtime. The exact prompt is `generation-prompt.txt`.

The previous fist, wing and beetle concepts remain in `../fantasy-v1/` for history. This batch uses new subjects and the game's actual weapon art as its style reference.
